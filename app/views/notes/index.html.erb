 <table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>
   <% @notes.each do |note| %>
     <tr>
       <td>
          <%= link_to note.title, note %>
          <%= shorten(note.body) %>
        </td>
      </tr>
    <% end %>
  <tr>
</table> 

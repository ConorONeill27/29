<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Bundles</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <!-- Using Google Font "Inter" -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap">
  <%= stylesheet_link_tag "marksmith", media: "all" %>
  <%= javascript_include_tag "marksmith" %>

  <%= yield :head %>

  <style>
    /* GLOBAL STYLES */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #0c0f17, #000);
      color: #fff;
    }
    /* NAVIGATION BAR */
    nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem 2rem;
      background: transparent;
    }
    .nav-left, .nav-right {
      display: flex;
      align-items: center;
    }
    .logo {
      font-size: 1.25rem;
      font-weight: 700;
      margin-right: 2rem;
    }
    .nav-links a {
      margin-right: 1.5rem;
      text-decoration: none;
      color: #ccc;
      font-weight: 500;
    }
    .nav-links a:hover {
      color: #fff;
    }
    .demo-btn {
      text-decoration: none;
      padding: 0.6rem 1.2rem;
      border: 2px solid #5865f2;
      border-radius: 6px;
      color: #fff;
      font-weight: 500;
      transition: background-color 0.3s ease;
    }
    .demo-btn:hover {
      background-color: #5865f2;
    }

    /* HERO SECTION */
    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      min-height: calc(100vh - 80px); /* Full viewport minus nav height */
      padding: 0 1rem;
    }
    .hero h1 {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }
    .hero p {
      max-width: 600px;
      margin-bottom: 2rem;
      line-height: 1.6;
      color: #ccc;
    }
    .hero-buttons {
      display: flex;
      gap: 1rem;
    }
    .btn {
      display: inline-block;
      text-decoration: none;
      background-color: #fff;
      color: #000;
      padding: 0.8rem 1.5rem;
      border-radius: 6px;
      font-weight: 500;
      transition: background-color 0.3s ease, color 0.3s ease;
    }
    .btn:hover {
      background-color: #ccc;
    }
  </style>
</head>
<body>
  <!-- NAVIGATION BAR -->
  <nav>
    <div class="nav-left">
      <div class="logo">Bundles</div>
      <div class="nav-links">
        <a href="#solutions">Solutions</a>
        <a href="#industries">Industries</a>
        <a href="#about">About Us</a>
      </div>
    </div>
    <div class="nav-right">
      <!-- "My Hub" button linking to the profiles index -->
      <%= link_to "My Hub", profiles_path, class: "demo-btn", data: { turbo: false } %>
    </div>
  </nav>

  <!-- HERO SECTION -->
  <section class="hero">
    <h1>Welcome to Bundles</h1>
    <p>
      Simplify your workflow and take your projects to the next level. 
      Explore solutions, industries, and more with just a few clicks.
    </p>
    <div class="hero-buttons">
      <a href="#explore" class="btn">Explore Solutions</a>
      <a href="#schedule" class="btn">Schedule a Demo</a>
    </div>
  </section>
</body>
</html>
